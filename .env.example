# Copie para .env e preencha. O .env NÃO é commitado (ver .gitignore).

# Discord
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_GUILD_ID=

# MongoDB
MONGO_URI=mongodb://user:pass@host:27017
MONGO_DB=wynn_guild

# WynnCraft
WYNN_GUILD_PREFIX=
# Opcional: se preenchido, a identificação da guilda passa a usar o UUID, que é
# imutável, em vez do prefixo (que o dono pode trocar a qualquer momento).
WYNN_GUILD_UUID=
# Opcional: eleva o limite de 50 para 120 req/min. Deixe vazio se não tiver.
WYNN_API_KEY=

# A black-list e as guildas aliadas NÃO ficam mais aqui. A lista vive no banco e
# é gerida em runtime, sem redeploy:
#
#   /guilds blacklist add tag:GsW
#   /guilds ally      add tag:XYZ
#   /guilds list

# Healthcheck HTTP (Easypanel). Porta interna do container.
PORT=8080

# Domínio público do bot (sem barra no fim). Necessário para o /modpack, que
# serve o mods.rar em ${PUBLIC_URL}/modpack, e usado também pelo dossiê em
# ${PUBLIC_URL}/gsw. Deve bater com o Host das labels do Traefik no
# docker-compose.yml. Em produção, defina na aba Environment do Dokploy.
PUBLIC_URL=https://bot.battomic.com

# Emotes customizados do servidor (formato <:nome:id> ou <a:nome:id> se animado).
# Sem valor, cai no fallback Unicode. Ex. do emote de guerra no painel de info:
EMOJI_WAR=
